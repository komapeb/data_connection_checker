name: data_connection_checker
description: A pure Dart utility library that checks for an internet connection by opening a socket to a list of specified addresses, each with individual port and timeout. Defaults are provided for convenience.
version: 0.2.1
homepage: https://github.com/komapeb/data_connection_checker
author: Kristiyan Mitev <me@example.com>

environment:
  sdk: '>=2.2.0 <3.0.0'

# dependencies:

dev_dependencies:
  test: ^1.0.0
  pedantic: ^1.7.0
